# oligarch: MSS Comanged control

# source control env
if [[ $HOSTNAME == 'oligarch'* ]]; then
    source ~mss-cm-control/ansible_venv/bin/activate
fi

# ansible config
export $ANSIBLE_CONFIG="~/ansible/ansible.cfg"
